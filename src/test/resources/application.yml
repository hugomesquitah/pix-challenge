app:
  version: test

spring:
  application:
    name: pix-challenge

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: 123456
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true